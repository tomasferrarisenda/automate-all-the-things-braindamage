apiVersion: ec2.aws.upbound.io/v1beta1
kind: Subnet
metadata:
  name: ${{ values.system}}-${{ values.service }}-public-subnet-b
spec:
  forProvider:
    # mapPublicIpOnLaunch: true
    availabilityZone: ${{ values.region }}b
    cidrBlock: 10.0.0.0/19
    region: ${{ values.region }}
    vpcIdRef: 
      name: ${{ values.system}}-${{ values.service }}-vpc
    tags:
      Name: ${{ values.system}}-${{ values.service }}-public-subnet-b
      kubernetes.io/role/internal-elb: "1" # Required for EKS
      kubernetes.io/cluster/${var.project}-cluster: "owned" # Required for EKS
  providerConfigRef:
    name: aws
---
apiVersion: ec2.aws.upbound.io/v1beta1
kind: RouteTableAssociation
metadata:
  name: ${{ values.system}}-${{ values.service }}-rta-public-b
spec:
  forProvider:
    region: ${{ values.region }}
    subnetIdRef:
      name: ${{ values.system}}-${{ values.service }}-public-subnet-b
    routeTableIdRef:
      name: ${{ values.system}}-${{ values.service }}-route-table-public
    tags:
      Name: ${{ values.system}}-${{ values.service }}-rta-public-b
  providerConfigRef:
    name: aws



